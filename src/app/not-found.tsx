import NotFoundPage from '@/components/NotFoundPage';

export default function NotFound() {
   return (
      <div className="flex flex-grow flex-col p-5 lg:p-10">
         <NotFoundPage icon="FileNotFound" title="Page" />
      </div>
   );
}
